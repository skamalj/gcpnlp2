steps:

- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', 'gcr.io/spinnaker/gke-firestore:$TAG_NAME','.']

- name: 'gcr.io/cloud-builders/docker'
  args: ['push', 'gcr.io/spinnaker/gke-firestore:$TAG_NAME']
